# Low-Code No-Code Rapid Prototyping — Course Stack
#
# Copy this file to .env, then replace change-me with a unique random password:
#   cp .env.example .env
#   openssl rand -hex 16
#
# Keep .env out of Git. This example file is safe to commit; your .env is not.

# Password for the course labs Postgres database (user: student; database: labs)
LABS_POSTGRES_PASSWORD=change-me
